# Copy to .env and fill in to enable the personalized (Claude) cue layer.
# Without a key the app still works — cached cues only (the designed degraded mode).
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...

# Optional: premium multilingual voices. Without it, on-device system voices are used.
EXPO_PUBLIC_ELEVENLABS_API_KEY=
